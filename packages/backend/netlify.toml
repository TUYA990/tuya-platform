[build]
  command = "npm install && npm run build"
  functions = "dist"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.js"
  status = 200

[env]
  DATABASE_URL = ""
  JWT_SECRET = ""
  NODE_ENV = "production"
  PORT = "8888"
